import { SomeSharedDirective } from './some-shared.directive';

describe('SomeSharedDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeSharedDirective();
    expect(directive).toBeTruthy();
  });
});
