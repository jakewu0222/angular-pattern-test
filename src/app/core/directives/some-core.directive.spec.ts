import { SomeCoreDirective } from './some-core.directive';

describe('SomeCoreDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeCoreDirective();
    expect(directive).toBeTruthy();
  });
});
